{
  "name": "taskflow",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "docker:build": "docker build -t taskflow-app .",
    "docker:run": "docker run -p 3000:80 taskflow-app",
    "docker:compose": "docker-compose up -d",
    "docker:compose-dev": "docker-compose up"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@react-buddy/ide-toolbox": "^2.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.3",
    "vite": "^4.4.5"
  }
}